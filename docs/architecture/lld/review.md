# LLD Review (quality gates + interview + trade-offs, no overengineering)

Gates pass: no cycles (graph in README/catalog); domain infra-free; boundaries explicit; authN/Z + limits + OTP + secrets + webhook-sig defined; collections+indexes+tx + concurrency/idem + outbox/inbox + retry/DLQ + versioning + dup-handling defined; timeouts/retries/degrade + failures; logs/metrics/traces/corrId/audit defined; test boundaries per level defined.
Interview answers (from this LLD): monolith (single tx/deploy, event-isolated extractables: search/notify/audit) not microservices (no org/scale trigger); Atlas (doc catalog + tx + Vercel pooling) not PG (join-heavy ledger absent); Redis ephemeral (coordination) + fail-closed money; Kafka only cross-domain + outbox (no dual-write) + inbox (dup-safe); oversell via cond-inc+TTL; pay-idem via key+unique+server-verify; limits via Redis sliding-window; Redis-down closed-auth/open-read; Kafka-down → outbox buffers + degrade notify; notify-fail → DLQ+replay; scale via partitions/tiers/Cache/CDN; strong (inv/pay/order) vs eventual (notify/analytics/search).
Trade-offs: monolith blast-radius vs tx-simplicity (chosen); Atlas joins vs flex (app-enforced); sync latency vs async complexity (Kafka minimal); embed snapshots (history) vs refs (lifecycle); optimistic-version vs locks (former; lock only proven coupon-hot); cache TTL vs freshness (money never cached). Nothing added without reason: 6 topics, one lock candidate, no K8s/ES/lock-service/saga.
