# API Examples (fake data; corrId `corr-ex-001` throughout)

1. Register 201 `{user:{id:'u_1',email:'ada@example.com',emailVerified:false}}`. 2. Login 200 `{accessToken:'eyJ…',expiresIn:900}` + refresh cookie. 3. OTP verify 200 `{emailVerified:true}`. 4. Product list 200 `{data:[{id:'p_1',slug:'pixel-8',title:'Pixel 8',brand:{name:'Google'},variants:[{sku:'PX8-128-BLK',price:{minor:69900,currency:'USD'},availability:'IN_STOCK'}]}],pagination:{nextCursor:'…',hasMore:true}}`. 5. Detail adds description/media/reviews-summary. 6. Add cart 201 `{itemId:'ci_1',qty:2,lineTotal:{minor:139800}}`. 7. Checkout 201 `{orderId:'o_1',orderNo:'ORD-2026-0001',status:'PENDING_PAYMENT',payment:{paymentId:'pay_1',status:'INITIATED'}}` (Idempotency-Key sent). 8. Payment poll 200 `{status:'CAPTURED'}`. 9. Order GET 200 full snapshots (items/price/address immutable). 10. Cancel 200 `{status:'CANCELLED'}` or 422 ORDER_NOT_CANCELLABLE. 11. Return 201 `{rmaId:'rma_1',state:'REQUESTED'}`. 12. Review 201 `{id:'rev_1',status:'pending'}`. 13. Admin product create 201 (admin JWT; audit logged). 14. Rate limit 429 `{"error":{"code":"RATE_LIMITED","message":"Too many requests.","details":[],"correlationId":"…"}}` + `Retry-After: 60`. 15. Validation 400 VALIDATION_ERROR with `details:[{field:'email',code:'INVALID_EMAIL',…}]`. 16. Idem conflict 409 IDEMPOTENCY_CONFLICT (same key, different body).
